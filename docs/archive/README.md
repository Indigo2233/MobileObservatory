# Archived design plans

These documents are **historical**. The work is either coded already or superseded.
Status and remaining hardware gaps live in [`../README.md`](../README.md).

Do not start new implementation from these files without checking the short status page in `docs/` first.
